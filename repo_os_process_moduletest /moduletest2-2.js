const blockclass = require("./moduleteset2-2");

const require = (path) => {
  // 1. 해당 파일의 path를 가져옴
  // 2. 해당 소스코드를 실행
  // 3. return module.exports
};

console.log(blockclass);
console.log(blockclass.obj);
console.log(blockclass.add);
console.log(blockclass.add2);
